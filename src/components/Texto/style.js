import styled from 'styled-components';
import {colors, fonts} from '~/src/styles/index'

export const CorpoTexto = styled.h1`
  margin-top: 30px;
  color: ${colors.corDoTexto};
  font-family: ${fonts.fontFamily.poppins};
  width: 100%;
  font-size: 20px;
  font-weight: 300;
  text-align: center;
`;